{
  "$schema": "../schemas/runtime-toggles.schema.json",
  "toggles": [
    {
      "module": "hook.pre-write-check",
      "envVar": "ORCHESTRA_HOOK_PRE_WRITE_CHECK",
      "default": "on",
      "description": "Blocks Write/Edit/MultiEdit containing detectable secrets and enforces frontmatter Gate-A/B/C/D. Disable for fixtures that must contain secret-shaped values (rare)."
    },
    {
      "module": "hook.post-bash-lint",
      "envVar": "ORCHESTRA_HOOK_POST_BASH_LINT",
      "default": "on",
      "description": "Surfaces source-modifying Bash commands (npm install, sed -i, etc.) to stderr. Observer; never blocks."
    },
    {
      "module": "hook.val-calibration",
      "envVar": "ORCHESTRA_HOOK_VAL_CALIBRATION",
      "default": "on",
      "description": "Injects calibration anchor into @evaluator Task prompts. Reads hooks/calibration/calibration-examples.md."
    },
    {
      "module": "hook.metrics-collector",
      "envVar": "ORCHESTRA_HOOK_METRICS_COLLECTOR",
      "default": "on",
      "description": "Local-only JSONL telemetry to <project>/.claude/.orchestra/metrics/events.jsonl. Disable for fully air-gapped runs."
    },
    {
      "module": "hook.post-write-puml",
      "envVar": "ORCHESTRA_HOOK_POST_WRITE_PUML",
      "default": "on",
      "description": "Renders .puml → .svg via plantuml CLI on PostToolUse(Write|Edit|MultiEdit). Non-blocking; warns to stderr when render fails or sibling .md lacks the expected ![..](diagrams/<name>.svg) reference."
    },
    {
      "module": "hook.agent-plan-sync",
      "envVar": "ORCHESTRA_HOOK_AGENT_PLAN_SYNC",
      "default": "on",
      "description": "Binds Claude Code task IDs to per-agent PLAN files at <cwd>/.orchestra/tasks/<run-id>/<agent>/<feature-id>.md — recomputes T-NNN counts, mirrors the ## Tasks checklist, and flips plan status. Disable for runs that don't author plan files."
    },
    {
      "module": "mcp.orchestra-fs",
      "envVar": "ORCHESTRA_MCP_ORCHESTRA_FS",
      "default": "on",
      "description": "Filesystem MCP exposing the `tree` tool. Disable in air-gapped or filesystem-restricted environments."
    },
    {
      "module": "mcp.orchestra-probe",
      "envVar": "ORCHESTRA_MCP_ORCHESTRA_PROBE",
      "default": "on",
      "description": "Runtime probes MCP (http_probe + db_state). Disable when network/database access is undesirable."
    },
    {
      "module": "skill.task-breakdown",
      "envVar": "ORCHESTRA_SKILL_TASK_BREAKDOWN",
      "default": "on",
      "description": "Decomposition + SP estimation for @lead's task graph generation."
    },
    {
      "module": "skill.code-review",
      "envVar": "ORCHESTRA_SKILL_CODE_REVIEW",
      "default": "on",
      "description": "Severity-graded review checklist consumed by @reviewer."
    },
    {
      "module": "skill.qa-test-planner",
      "envVar": "ORCHESTRA_SKILL_QA_TEST_PLANNER",
      "default": "on",
      "description": "Test plan + coverage + adversarial fuzz inputs consumed by @test."
    },
    {
      "module": "skill.write-contract",
      "envVar": "ORCHESTRA_SKILL_WRITE_CONTRACT",
      "default": "on",
      "description": "openapi description: schema + Probe DSL authoring guide consumed by @lead."
    },
    {
      "module": "skill.c4-architecture",
      "envVar": "ORCHESTRA_SKILL_C4_ARCHITECTURE",
      "default": "on",
      "description": "C4 L1+L2 + Logical ERD + Inter-service Sequence diagram authoring guide consumed by @architect."
    },
    {
      "module": "skill.plantuml",
      "envVar": "ORCHESTRA_SKILL_PLANTUML",
      "default": "on",
      "description": "PlantUML grammar + render-target authoring guide consumed by @architect, @lead."
    },
    {
      "module": "skill.java-development",
      "envVar": "ORCHESTRA_SKILL_JAVA_DEVELOPMENT",
      "default": "on",
      "description": "Java/Spring read-side intel + write-side conventions consumed by @backend on Java/Spring projects."
    },
    {
      "module": "config.metrics-cost-banner",
      "envVar": "ORCHESTRA_METRICS_COST_BANNER",
      "default": "off",
      "description": "Emit a one-line token + USD cost banner at the end of every /orchestra run (Step 7 closure). Reads cost_usd persisted by the metrics-collector hook from runs/<id>.json. config.* namespace has no install-modules counterpart — it's a dispatcher-side display toggle only."
    }
  ]
}
