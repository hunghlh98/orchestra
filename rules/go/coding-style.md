---
paths:
  - "**/*.go"
---
# Go coding-style

> v1.0.0 ships a stub. Full ruleset in v1.1+.

This file is a placeholder so:
- `paths:` activation works (validate.js sees the frontmatter)
- The plugin's language coverage is documented
- Contributors know where to add Go-specific rules

For now: follow the standard `gofmt` and `go vet` conventions.
See <https://go.dev/doc/effective_go> for canonical guidance.
