---
paths:
  - "**/*.py"
---
# Python coding-style

> v1.0.0 ships a stub. Full ruleset in v1.1+.

This file is a placeholder so:
- `paths:` activation works (validate.js sees the frontmatter)
- The plugin's language coverage is documented
- Contributors know where to add Python-specific rules

For now: follow the standard `black` formatter and `ruff` linter conventions.
See <https://peps.python.org/pep-0008/> for canonical guidance.
