---
paths:
  - "**/*.kt"
  - "**/*.kts"
---
# Kotlin coding-style

> v1.0.0 ships a stub. Full ruleset in v1.1+.

This file is a placeholder so:
- `paths:` activation works (validate.js sees the frontmatter)
- The plugin's language coverage is documented
- Contributors know where to add Kotlin-specific rules

For now: follow the standard `ktlint` and `detekt` conventions.
See <https://kotlinlang.org/docs/coding-conventions.html> for canonical guidance.
