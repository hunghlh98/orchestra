{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "orchestra:runtime-toggles",
  "title": "orchestra runtime-toggles manifest",
  "description": "Env-var opt-out registry. Each entry pairs a module-or-config-namespace identifier with an ORCHESTRA_* env var and on/off default.",
  "type": "object",
  "required": ["toggles"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "toggles": {
      "type": "array",
      "items": { "$ref": "#/$defs/toggle" }
    }
  },
  "$defs": {
    "toggle": {
      "type": "object",
      "required": ["module", "envVar", "default", "description"],
      "additionalProperties": false,
      "properties": {
        "module": {
          "type": "string",
          "pattern": "^[a-z]+\\.[a-z][a-z0-9-]*$",
          "description": "Either an install-modules entry name (hook.* / skill.* / mcp.*) or the reserved config.* prefix for dispatcher-side display toggles."
        },
        "envVar": {
          "type": "string",
          "pattern": "^ORCHESTRA_[A-Z_]+$"
        },
        "default": { "enum": ["on", "off"] },
        "description": { "type": "string" }
      }
    }
  }
}
